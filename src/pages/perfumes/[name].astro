---
import Layout from '../../layouts/Layout.astro'
export const getStaticPaths = () => {
  return [
    {
      params: { name: 'lady' },
      props: { title: 'ladies' },
    },
  ]
}
getStaticPaths()
const { title } = Astro.props
---

<Layout>
  <h1>{title}</h1>
</Layout>
