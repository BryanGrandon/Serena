---
import Navigation from '../ui/Navigation'

type Props = {
  title?: string
  description?: string
}

const { title, description } = Astro.props
---

<header class=''>
  <Navigation />
  <article class='flex flex-col items-center justify-center gap-4 p-4 min-h-[60vh]'>
    <h1 class='font-basicaline text-5xl font-bold text-center'>{title}</h1>
    <p class='prose prose-lg dark:prose-invert'>{description}</p>
  </article>
</header>
